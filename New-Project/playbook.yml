---
- name: Pre Configure
  become: yes
  hosts: "{{ target }}"
  roles:
    - Config
  
  
  
- name: Install Docker
  become: yes
  hosts: "{{ target }}"
  roles:
    - Docker

- name : Install a role
  become: yes
  hosts: "{{ target }}"
  roles:
    - roleIn
